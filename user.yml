#product服务所有环境都使用的配置就放到product.yml这个配置文件里
logging:
  file: /usr/local/tomcat/USER.log
  pattern:
    level: info
    file: "[ORDER] [%d{yyyy-MM-dd HH:mm:ss.SSS}] [%thread] %-5level = > %msg%n"
  level:
    com:
      shyfay:
        user:
          repository: debug
